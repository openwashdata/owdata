# Upstream fixes for the data repositories

Generated by data-raw/upstream_fixes.R from the harvest of 2026-07-20. Each row is a defect in a data package repository that the owdata
harvester tolerates but that should be fixed at the source. This list
seeds the migration campaign (openwashdata/owdata#10); regenerate it
after each harvest and delete rows as upstream PRs merge.

Every fix is an author-side change; announce the campaign to
maintainers before opening scripted PRs (see the premortem notes on
issue #10).

32 defects across 27 repositories.

## basisghana

- `data-raw/dictionary.csv`: space-padded column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## biogasoutcomesmalawi

- `data-raw/dictionary.csv`: space-padded column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## boreholeforensicspumpingtest

- `R/boreholeforensicspumpingtest.R and data-raw/dictionary.csv`: @format says 38 variables, dictionary lists 44 for boreholeforensicspumpingtest. Fix: Reconcile the documentation with the dictionary.

## boreholelabdata

- `data-raw/dictionary.csv`: file is not UTF-8 encoded (latin1). Fix: Re-save the file as UTF-8.

## cbssuitabilityhaiti

- `R/okap.R and data-raw/dictionary.csv`: @format says 21 variables, dictionary lists 13 for okap. Fix: Reconcile the documentation with the dictionary.

## chckapmalawi

- `data-raw/dictionary.csv`: quoted column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## cleanteamghana

- `data-raw/dictionary.csv`: quoted column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## climatevulnerabilitykhm

- `data-raw/dictionary.csv`: doubly quoted column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## dryingfaecal

- `data-raw/dictionary.csv`: starts with a UTF-8 BOM. Fix: Re-save without BOM.
- `data-raw/dictionary.csv`: deprecated two-column schema. Fix: Regenerate with the current washr template (five clean columns).

## floodresponsemw

- `data-raw/dictionary.csv`: trailing empty columns in the header. Fix: Regenerate with the current washr template (five clean columns).

## fslogisticskampala

- `data-raw/dictionary.csv`: quoted column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## glaas

- `data-raw/dictionary.csv`: file missing in a reviewed-tier package. Fix: Add the five-column dictionary (washr template).

## grdwtrsmpkwale

- `CITATION.cff`: version 0.0.0.9000 disagrees with DESCRIPTION 0.0.1. Fix: Run washr::update_citation() after the next version bump.
- `data-raw/dictionary.csv`: nonstandard header: directory,file_name,variable_name,variable_type,description,unit_type,error. Fix: Regenerate with the current washr template (five clean columns).

## hydrogeologicalghana

- `DESCRIPTION`: DESCRIPTION URL does not mention https://github.com/openwashdata/hydrogeologicalghana. Fix: Point the URL field at the package repository.

## planetaryhdi

- `R/planetaryhdi.R and data-raw/dictionary.csv`: @format says 11 variables, dictionary lists 12 for planetaryhdi. Fix: Reconcile the documentation with the dictionary.
- `data-raw/dictionary.csv`: file is not UTF-8 encoded (latin1). Fix: Re-save the file as UTF-8.

## portawaterperu

- `CITATION.cff`: version 0.0.0.9000 disagrees with DESCRIPTION 0.0.1. Fix: Run washr::update_citation() after the next version bump.

## rwpfunctionality

- `R/rwpfunctionality.R and data-raw/dictionary.csv`: @format says 52 variables, dictionary lists 51 for rwpfunctionality. Fix: Reconcile the documentation with the dictionary.

## thyolocbcc

- `data-raw/dictionary.csv`: trailing empty columns in the header. Fix: Regenerate with the current washr template (five clean columns).

## undpcomposite

- `data-raw/dictionary.csv`: file is not UTF-8 encoded (latin1). Fix: Re-save the file as UTF-8.

## washinvestments

- `R/washinvestments.R and data-raw/dictionary.csv`: @format says 52 variables, dictionary lists 40 for washinvestments. Fix: Reconcile the documentation with the dictionary.

## washmalawi

- `R/washmalawi.R and data-raw/dictionary.csv`: @format says 27 variables, dictionary lists 26 for washmalawi. Fix: Reconcile the documentation with the dictionary.

## washopenresearch

- `R/uncnewsletter.R`: no parseable @format dimensions for uncnewsletter. Fix: Add '@format A tibble with N rows and M variables'.
- `R/washdev.R`: no parseable @format dimensions for washdev. Fix: Add '@format A tibble with N rows and M variables'.

## waterconsumersurvey

- `data-raw/dictionary.csv`: trailing empty columns in the header. Fix: Regenerate with the current washr template (five clean columns).

## waterpointstatus

- `DESCRIPTION`: normalized malformed version 'v1.0.0'. Fix: Remove the leading v from the Version field.

## waterpumpkwale

- `R/weeklyvol2014.R and data-raw/dictionary.csv`: @format says 53 variables, dictionary lists 2 for weeklyvol2014. Fix: Reconcile the documentation with the dictionary.
- `R/weeklyvol2015.R and data-raw/dictionary.csv`: @format says 53 variables, dictionary lists 2 for weeklyvol2015. Fix: Reconcile the documentation with the dictionary.

## whatawaste

- `data-raw/dictionary.csv`: quoted column names in the header. Fix: Regenerate with the current washr template (five clean columns).

## wsabrazil

- `data-raw/dictionary.csv`: stored as a git-lfs pointer. Fix: Untrack the file from git-lfs and commit the plain CSV.

